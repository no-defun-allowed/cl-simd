#!/bin/sh
#|-*- mode:lisp -*-|#
#|
exec ros -Q -- $0 "$@"
|#

(defun main (&rest argv)
  (declare (ignorable argv))
  (uiop:run-program "make")
  (handler-case
      (asdf:test-system :cl-simd)
    (serious-condition (c)
      (describe c)
      (uiop:quit 2))))




